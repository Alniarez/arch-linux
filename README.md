This is my arch linux post installations scripts. It is intended to be used on top of a minimal KDE plasma desktop enviroment, but it should wotk with other DE.

Ussage:
```
curl -L https://github.com/Alniarez/arch-linux/archive/refs/heads/main.tar.gz | tar -xz
cd arch-linux-main
sh install.sh
```
